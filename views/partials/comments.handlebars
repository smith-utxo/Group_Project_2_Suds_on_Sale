<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div class="text">
      <i class="fa-solid fa-comment"></i>
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>